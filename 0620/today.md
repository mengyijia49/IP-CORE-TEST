师兄的测试脚本：

```tcl

```

我们只做有自带testbench的IP的测试。

先检查同一IP核的两个不同配置生成的配置是否一致，一致的话，没什么好说的了。

哪些IP可以自动生成testbenh文件：

| 可以 | 不可以 |
| ---- | ------ |
|      |        |
|      |        |
|      |        |



不一致的话，试图找一下，有没有不影响功能，只影响实现方式的配置。

哪些配置会改变IP核的功能：

| 会改变 | 不会改变 |
| ------ | -------- |
|        |          |
|        |          |
|        |          |

